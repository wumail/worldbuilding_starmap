# V1/V2 分类恢复与科学状态核验

2026-09-15。保留按用途划分的 `src/`、`data/`、`output/`、`reference/`、`design/`，只重新明确网页版本和公共模块。

## 交付结构

- `web/v1/`：标准星图的平面、沉浸页面与专属渲染/状态/控制。
- `web/v2/`：肉眼观看的两个页面与专属渲染、状态、测试、历史验证资料。
- `web/shared/`：公共恒星系参数、Kepler 计算、星表加载、投影、点星采样、星云弥散光、黄道天区和界面样式。
- 根 `index.html`：明确的版本选择页。品牌链接返回首页，两版可直接打开对应的另一版视图。
- 旧 `web/sky_atlas.html`、`web/star_map.html`、`web/eye/` HTML 入口保留跳转，查询参数和锚点保留。没有恢复旧 `v4/` 目录。GitHub Pages 的历史路径另由自定义 404 处理。

两版不再重复保存 `solar_system.mjs`。观察设置键保持不变，V1/V2 仍各自保存状态。更新了页面过时的旧星数和“未按新银心距离采样”说明。

## 已执行检查

| 项目 | 结果 |
|---|---|
| Node 计算、投影、资源、版本设置、旧 URL 路由 | 64/64 通过 |
| 原 Python 科学与生成测试 | 49/49 通过；另加入初始倾角检查，与原两项自转测试一起 3/3 通过，共 50 项不同检查 |
| V1 图形回归 | 38/38 通过 |
| V2 图形与交互回归 | 48/48 通过 |
| 深空方向、亮度、遮挡与投影 | 6/6 通过 |
| 60 秒平面连续播放及恢复 | 5/5 通过 |
| 四个正式入口 | 均加载 9,356 点源、8,947 深空记录；天区及深空开关成功，没有页面脚本错误或本地 HTTP 错误 |
| 文件保持 | 58 份生成源码、参考文件、输出、PARSEC 数据和既有积分资料指纹不变 |

[浏览器状态与指标](browser.json)、[文件指纹](preservation.json)、[移动映射](moved_files.json)保留具体证据。旧验证资料保留历史路径身份，没有改写其历史指纹。目录移动后的相对模块引用和静态资源地址已检查，`git diff --check` 通过。

## 科学状态

默认配置和当前展示批次都使用 **30,712 光年 = 9,416.34200594183 pc**。使用当前 `src/` 完整重生成原种子后，点源、银河/尘埃、星团与星云物理内容的规范化散列全部一致，见 [完整复现记录](../galactic_reproduction_20260915.json)。重生成只在内存中进行，没有覆盖星表或改写原产物的来源指纹。

星云、星团算法仍为 4.4 的 `PARSEC_Poisson_IMF_Plummer_caseB_v1`。科学方法与适用范围见 [科学说明](../galactic_sky_science.md)；模型先验和近似仍然公开，不把它们误称为唯一正确的星系地图。

用户已选择双月倾角为第 0 日相对初始赤道的瞬时值。候选模型本来就符合此约定，本次独立检查初值并重读两档完整轨迹，补充天空方向、轨道法线、偏心率及采样最近距离的比较。约四万年进动仍是量级候选，后期双月星历尚未收敛，未替换正式 Kepler 运动。见 [更新后的设计说明](../../design/terrax_precession_nutation.md)与 [轨迹复核结果](../../design/spin_epoch_verification.json)。
